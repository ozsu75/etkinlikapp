<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - EtkinlikApp</title>
    <%- include('partials/head') %>
</head>
<body>
    <%- include('partials/header') %>
    
    <main class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-6 text-center">
                <h1 class="display-1 text-danger">Hata!</h1>
                <h2 class="mb-4"><%= message %></h2>
                
                <% if (typeof error !== 'undefined' && error.stack) { %>
                    <div class="card mt-4">
                        <div class="card-header">
                            <h5 class="mb-0">Hata Detayları (Sadece Geliştirme Ortamında)</h5>
                        </div>
                        <div class="card-body">
                            <pre class="text-start"><code><%= error.stack %></code></pre>
                        </div>
                    </div>
                <% } %>
                
                <div class="mt-4">
                    <a href="/" class="btn btn-primary me-2">Ana Sayfa</a>
                    <a href="javascript:history.back()" class="btn btn-secondary">Geri Dön</a>
                </div>
            </div>
        </div>
    </main>
    
    <%- include('partials/footer') %>
</body>
</html>